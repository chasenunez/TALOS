[package]
name = "talos"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
ratatui = "0.29"
rayon = "1"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
